{
    "type":"object",
    "$id":"schema:user:headers",
    "required":["Authorization"],
    "additionalProperties":false,
    "properties":{
        "authorization":{
            "type":"string",
            "minLength":10,
            "pattern":"^Bearer\\s.+$"
        }
    }
}